# Cache hashed assets forever (Vite generates unique filenames per build)
<IfModule mod_headers.c>
  <FilesMatch "^.+-[A-Za-z0-9]+\.(js|css|woff2?|png|jpg|jpeg|gif|svg|ico|webp)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>
